import 'package:flutter/material.dart';

/// App primary color (adjust the hex value to your brand color)
const Color kPrimaryColor = Color(0xFF6200EE);

// Add a lowercase alias to match usage in other files (e.g., kprimaryColor)
const Color kprimaryColor = kPrimaryColor;